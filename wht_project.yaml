# Project name
name: feature_store_template
# Project's yaml schema version
schema_version: 4
# WH Connection Profile to use.
profile: kuldeep_test_fs

# Model folders to use.
model_folders:
  - models

# Entities in this project and their ids.
entities:
  - name: user
    id_types:
      - name: user_id
      - name: anonymous_id
      - name: email
      - name: main_id