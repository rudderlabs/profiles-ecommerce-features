Check the template based on latest wht build in `schema_ver_9` folder. With newer versions, only one active folder would be maintained and older versions would move to the `old_schema_vers` directory. 